# Migration `20200515202245-create-databae`

This migration has been generated by ChaMatt at 5/15/2020, 8:22:45 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"email" text  NOT NULL ,"id" text  NOT NULL ,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Project" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"description" text  NOT NULL ,"id" text  NOT NULL ,"title" text  NOT NULL ,"userId" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Task" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"description" text   ,"id" text  NOT NULL ,"priority" text  NOT NULL DEFAULT 'none',"projectId" text   ,"title" text  NOT NULL ,"userId" text   ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Project" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Task" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Task" ADD FOREIGN KEY ("projectId")REFERENCES "public"."Project"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200515202245-create-databae
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,38 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id       String    @default(uuid()) @id
+  email    String    @unique
+  name     String
+  projects Project[]
+  tasks    Task[]
+}
+
+model Project {
+  title       String
+  description String
+  id          String   @default(uuid()) @id
+  createdAt   DateTime @default(now())
+  User        User?    @relation(fields: [userId], references: [id])
+  userId      String?
+  tasks       Task[]
+}
+
+model Task {
+  id          String   @default(uuid()) @id
+  createdAt   DateTime @default(now())
+  title       String
+  description String?
+  priority    String   @default("none")
+  User        User?    @relation(fields: [userId], references: [id])
+  userId      String?
+  Project     Project? @relation(fields: [projectId], references: [id])
+  projectId   String?
+}
```


